// Max-Heap

#include <iostream>
#include <vector>

using namespace std;
class MaxHeap {
    public:
    int size;
    int arr[]

};

int main() {
    MaxHeap maxHeap();
    // maxHeap.insert(10);
    // maxHeap.insert(20);
    // maxHeap.insert(30);
    // maxHeap.insert(40);
    // maxHeap.insert(50);
    // maxHeap.insert(60);
    // maxHeap.insert(25);
    // maxHeap.display();
    // maxHeap.deleteMax();
    // maxHeap.display();
    return 0;
}